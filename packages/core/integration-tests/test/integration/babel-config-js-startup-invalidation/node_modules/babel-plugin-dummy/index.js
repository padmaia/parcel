module.exports = (api, opts) => {
  return {
    visitor: {
      StringLiteral(path, state) {
        const opts = state.opts;

        if (path.node.value === 'REPLACE_ME') {
          path.node.value = `This file was built on ${opts.date}`;
        }
      }
    }
  };
};
